<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.fyg.cuadrillas.dao.resources.UsuarioNTx">
<!-- Consulta Usuario-->
<select id="consultaUsuario" parameterType="usuario" resultType="usuario">
SELECT estatus,usuario FROM usuarios WHERE usuario = #{usuario}
</select>

<!-- Consulta Usuario-->
<select id="loginUsuario" parameterType="usuario" resultType="usuario">
SELECT usuario,contrasena FROM usuarios WHERE usuario = #{usuario} AND contrasena = #{contrasena}
</select>
</mapper>