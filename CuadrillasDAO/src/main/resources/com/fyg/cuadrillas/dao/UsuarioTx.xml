<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.fyg.cuadrillas.resources.UsuarioTx">
<!--- INSERTA EN LA TABLA USUARIO-->
<insert id="insertaUsuario" parameterType="usuario">
INSERT INTO usuarios(usuario,id_empleado,id_perfil,contrasena,cambio_contrasena,fecha_ult_acceso,fecha_alta,fecha_ult_mod,estatus)
VALUES(#{usuario},#{idEmpleado},#{id_perfil},#{contrasena},#{cambio_contrasena},now(),now(),now(),#{estatus})
</insert>

<!--- BAJA EN LA TABLA USUARIO-->

<update id="inactivaUsuario" parameterType="usuario">
UPDATE usuarios SET
estatus = #{estatus},
fecha_ult_mod = now()
WHERE usuario = #{usuario}
</update>
</mapper>