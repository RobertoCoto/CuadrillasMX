<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.fyg.cuadrillas.resources.EmpleadoNTx">
	
<select id="consultaEmpleado" parameterType="empleado" resultType="empleado">
SELECT nombre,apellido_pat,apellido_mat,sexo,rfc,estatus,nss,fecha_nacimiento,telefono,observaciones,fecha_ingreso,codigo_vialidad,
codigo_area,codigo_puesto,sueldo,telefono,curp FROM empleado 
<where>
<if test="id_empleado !=null">
 id_empleado = #{idEmpleado}
 </if>
 <if test="nombre != null">
 AND nombre like #{nombre}
  </if>
 <if test="apellido_pat !=null">
 AND apellido_pat like #{apellidoPat}
 </if>
 <if test="apellido_mat !=null">
 AND apellido_mat like #{apellidoMat}
 </if>
 <if test="rfc !=null">
 AND rfc like #{rfc}
 </if>
</where>
</select> 
   
</mapper>